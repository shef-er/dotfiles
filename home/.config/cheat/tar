---
tags: [ archive ]
---
# Compress .tar
tar -v -pcf archive.tar ./target
# Extract .tar
tar -v -xf ./archive.tar

# Compress .tar.gz
tar -v -pczf archive.tar.gz ./target
# Extract .tar.gz
tar -v -xzf ./archive.tar.gz

# Compress .tar.bz
tar -v -pcjf archive.tar.bz2 ./target
# Extract .tar.bz, .tar.bz2, .tar.bzip2, .tbz2, .tb2, .tar.bz, .tbz
tar -v -xjf ./archive.tar.bz

# Compress .tar.xz
tar -v -pcJf archive.tar.xz ./target
# Extract .tar.xz
tar -v -xJf ./archive.tar.xz
